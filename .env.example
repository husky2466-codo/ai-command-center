# AI Command Center - Environment Variables

# Anthropic API Key (for Claude models)
ANTHROPIC_API_KEY=sk-ant-xxx

# OpenAI API Key (for GPT models)
OPENAI_API_KEY=sk-xxx

# HuggingFace Token (for HF models)
HF_TOKEN=hf_xxx

# Google OAuth2 Credentials
# Get these from: https://console.cloud.google.com/
# 1. Create a new project
# 2. Enable Gmail API, Calendar API, People API
# 3. Create OAuth 2.0 Client ID (Desktop app type)
# 4. Add redirect URI: http://localhost:8123/callback
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# HTTP API Server Configuration
# The local API server allows external tools (like Claude Code) to control the app
API_SERVER_PORT=3939
API_SERVER_KEY=your-secret-key-here  # Optional: Leave blank to disable authentication

# Note: This file should be copied to:
# 1. D:\OneDrive\Claude Config (Windows)\.env (primary)
# 2. ~/.env (fallback)
