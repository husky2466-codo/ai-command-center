---
description: Query OpenAI GPT models
argument-hint: <your question>
---

Use the OpenAI API to query GPT-4. The API key is stored in ~/.env as OPENAI_API_KEY.

Make an API call to OpenAI's chat completions endpoint with the user's question: $ARGUMENTS

Return the response to the user.
