---
description: Scrape a webpage using Playwright
argument-hint: <url>
---

Use Playwright to scrape the webpage at: $ARGUMENTS

1. Launch a headless browser
2. Navigate to the URL
3. Wait for the page to load
4. Extract the main content (title, headings, paragraphs, links)
5. Return a structured summary of the page content

If no URL is provided, ask the user for one.
